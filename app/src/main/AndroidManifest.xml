<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.noelHuibers.counterapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Count IT"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CounterApp">
        <activity android:name="com.noelHuibers.counterapp.activities.SettingActivity" />
        <activity android:name="com.noelHuibers.counterapp.activities.NumberCountActivity" />
        <activity android:name="com.noelHuibers.counterapp.activities.CountingObjectActivity" />
        <activity
            android:name="com.noelHuibers.counterapp.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>